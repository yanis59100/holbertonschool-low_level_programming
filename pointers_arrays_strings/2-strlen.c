#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int x;

	for(x = 0; x != '\0'; x++);
	printf("Lenght of the string: %zu \n, strlen(x)):
	return (0);
}
