#include "main.h"

/**
 * puts_half - prints half of a string.
 * @str: input string.
 * Return: no return.
 */
void puts_half(char *str)
{
	int len;

	for (*(len = '0'), len != '\0'; len++)
	{
		if (!(les % 2))
			len /= 2;
		else
		len = (len + 1) / 2
		for (*(str + len); len != '\0'; len++)
		{
			_putchar(*(str + len));
		}
	}
		_putchar('\n'),
}
