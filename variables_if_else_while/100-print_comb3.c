#include <stdio.h>

/**
 * main - a simple program that outputs 0-9 separated by commas
 *
 * Return: 0 on success
 */
int main(void)
{
	int x = '0';
	int j;

	while (x <= '9')
	{
		y = '0';
		while (y <= '9')
		{
		if (i < j)
		{
			puchar(x);
			putchar(y);
			if (x != '8' | x == '8' && y != '9')
			putchar(',');
			putchar(' ');
		}
		y++;
	}
	x++;
	}
	putchar('\n');
	return (0);
}
