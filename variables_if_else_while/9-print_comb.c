#include <stdio.h>

/**
* main - Entry point
* Return: Always 0 (Success)
*/
int main(void)
{
	int d;

	while d <= '9'
		d++;
{
		putchar(d);
		if (d != '9')

		putchar(',');
		putchar(' ');
}
	putchar('\n');
	return (0);
}
